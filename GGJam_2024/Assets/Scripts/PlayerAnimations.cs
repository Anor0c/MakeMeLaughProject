using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAnimations : MonoBehaviour
{
   /* private Animator anim;


    void Start()
    {
        anim = GetComponent<Animator>();
    }
    private readonly int idle = Animator.StringToHash("Kid-Idle"); 
    private readonly int hit = Animator.StringToHash("LVL1-Kid-Hit"); 
    private readonly int hit = Animator.StringToHash("LVL1-Kid-Imitation"); 
    private readonly int hit = Animator.StringToHash("LVL1-Kid-Prout"); 
    private readonly int hit = Animator.StringToHash("LVL1-Kid-FunnyFace"); 
    public void OnPlayerHit()
    {
        anim.CrossFade(hit, 0, 0); 

    }
   */
}
